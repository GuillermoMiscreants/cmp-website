# Astro origin the Presentation tool loads in its iframe. No trailing slash.
# Use http://127.0.0.1:4321 when previewing the local Astro server.
# The deployed preview worker is the server build, not the static production site.
SANITY_STUDIO_PREVIEW_ORIGIN=https://cmp-website-preview.guillermo-casanova.workers.dev
